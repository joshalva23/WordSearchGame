# About
A simple WordSearch Game.
  
## Commands for Use
### `npm i` to install packages
### `npm run start` to start local development server at localhost:3000
### `npm run build` to create a static build
### `npm run staticstart` to run static build
